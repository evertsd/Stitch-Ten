<% @categories.each do |category| %>
	<a href="/category/<%= category.name %>"  class="gallery-link" id="<%= (params[:name] == category.name) ? 'notify' : '' %>"><%= category.name.capitalize %></a>
<% end %>